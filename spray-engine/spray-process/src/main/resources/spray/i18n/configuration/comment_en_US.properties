executor.thread.concurrency.max=max thread concurrency of every executor. 20 to 100 is recommended, currently is {}, default is {}
executor.thread.capacity.max=max thread capacity of every executor, dispatch action will be blocked once the capacity is reached. 50 to 200 is recommended, currently is {}, default is {}
executor.remoting.support=whether to support remoting executor, currently is {}