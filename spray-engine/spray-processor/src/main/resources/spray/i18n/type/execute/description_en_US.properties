step_status.execution_suspending=step executor is suspending
step_status.manual_stop=step executor was stopped manually
step_status.execution_done=step executor was stopped successfully
step_status.execution_running=step executor is still running
step_status.execution_failed=step executor was stopped with failures
step_status.error_stop=step executor was stopped with errors
active_type.active=this step will be executed
active_type.skip=this step won't be executed but its next nodes may be
active_type.break=this step and its next nodes won't be executed
type.source=the step which is collecting data
type.compute=the step which is computing data
type.target=the step which is storing data

record.type.before=record before execute
record.type.success=record after success
record.type.failed=record after failed